# Touchline handoff

## GitHub Pages edition

The user requested GitHub Pages. A separate static Vite entry under `github-pages/` now reuses the shared Notebook with transactional IndexedDB storage, clear device-local saving copy and base-safe links. The default authenticated Sites adapter remains in place. `.github/workflows/pages.yml` builds/tests and publishes `dist-pages` from main; Pages is enabled in Actions mode at `https://frankyface.github.io/touchline/`. See `docs/github-pages.md` for implementation, tests and storage limits. This edition does not sync account notebooks automatically.

## Current checkpoint — research update

The user requested deep product research, improvements and pushing the work to `https://github.com/Frankyface/touchline`. The baseline has been pushed. All research improvements are implemented: starters, precise movement editing, mirroring, presentation/player focus, library retrieval, session handoff/coaching details, contextual coach mode, reflection/reuse and safe recovery copies. See `docs/product-research.md`.

Nineteen model/workflow tests, final uncached TypeScript, local API integration and production build passed. Two independent read-only reviewers approved the corrected source. No new browser QA was performed this turn; initial-release evidence below is historical.

Research update delivery is complete: version 2 deployed successfully from `86efa52ccde7b4a13a99f1f6fcbb533c46568954`, pushed to GitHub and Sites. See `docs/delivery.md` for exact IDs and verification limits. Preserve owner-only access and reuse the existing Site ID below. The later documentation receipt does not change the published product source.

## Initial release checkpoint (historical)

Updated: 2026-09-15. The user delegated a complete surprise build and waived the interview gate.

Workspace: `C:\Users\mbuby\OneDrive\Documents\.ClaudeProjects\Touchline`

## Implemented and verified
F001–F004 implemented: editable/animated rugby diagrams, durable private playbook, session planner/timer, print previews, SVG/JSON export and validated backup import. Root owns all product source. Independent read-only reviewer approved integrated implementation after six correctness repairs. Production build, TypeScript, seven model tests, local API integration and actual desktop/mobile interactions passed. See `docs/verification.md` for evidence and native print/download verification limits.

## Delivery checkpoint
F005 publication is the remaining step at this source snapshot. Site identity is saved in `.openai/hosting.json`: `appgprj_6aa9a0abd3e88191bb067d7d04e00ba9`. Owner-only access was verified and must remain private. Never create another Site for this checkout. Push source, package validated output using the current Sites helper, save the exact commit and deploy its saved version. Record the terminal result in `docs/delivery.md` after publication. If that record exists it supersedes this checkpoint.

The existing local preview is on port 5178. Stop it after a successful hosted handoff. No automation or ongoing background task is required. Runtime credentials remain ephemeral and must never enter source files. Current Sites helper is under `C:\Users\mbuby\.codex\plugins\cache\openai-bundled\sites\0.1.66`.

No task token budget was requested; authoritative token usage is unavailable. See `docs/token_usage.md`. All five allowed questions have been answered. Continue routine delivery without asking further questions.
